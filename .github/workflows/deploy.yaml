name: Deploy MultiDocker
on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - run: docker login -u ${{ secrets.DOCKER_USERNAME }} -p ${{ secrets.DOCKER_PASSWORD }}
      - run: docker build -t a99nabilalqemam/react-test -f ./client/Dockerfile.dev ./client
      - run: docker run -e CI=true a99nabilalqemam/react-test npm test

      - run: docker build -t a99nabilalqemam/multi-client ./client
      - run: docker build -t a99nabilalqemam/multi-nginx ./nginx
      - run: docker build -t a99nabilalqemam/multi-server ./server
      - run: docker build -t a99nabilalqemam/multi-worker ./worker

      - run: docker push a99nabilalqemam/multi-client
      - run: docker push a99nabilalqemam/multi-nginx
      - run: docker push a99nabilalqemam/multi-server
      - run: docker push a99nabilalqemam/multi-worker

      - name: Generate deployment package
        run: zip -r deploy.zip . -x '*.git*'

      - name: Deploy to EB
        uses: einaregilsson/beanstalk-deploy@v18
        with:
          aws_access_key: ${{ secrets.AWS_ACCESS_KEY }}
          aws_secret_key: ${{ secrets.AWS_SECRET_KEY }}
          application_name: complex-docker
          environment_name: Complex-docker-env
          existing_bucket_name: elasticbeanstalk-eu-north-1-974061962186
          region: eu-north-1
          version_label: ${{ github.sha }}
          deployment_package: deploy.zip
