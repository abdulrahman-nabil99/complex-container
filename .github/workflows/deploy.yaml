name: Deploy MultiDocker
on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - run: docker login -u ${{ secrets.DOCKER_USERNAME }} -p ${{ secrets.DOCKER_PASSWORD }}
      - run: docker build -t a99nabil/react-test -f ./client/Dockerfile.dev ./client
      - run: docker run -e CI=true a99nabil/react-test npm test

      - run: docker build -t a99nabil/multi-client ./client
      - run: docker build -t a99nabil/multi-nginx ./nginx
      - run: docker build -t a99nabil/multi-server ./server
      - run: docker build -t a99nabil/multi-worker ./worker

      - run: docker push a99nabil/multi-client
      - run: docker push a99nabil/multi-nginx
      - run: docker push a99nabil/multi-server
      - run: docker push a99nabil/multi-worker
