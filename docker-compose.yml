version: "3"
services:
  client:
    image: "a99nabilalqemam/multi-client"
    mem_limit: 128m
    hostname: client
  express:
    image: "a99nabilalqemam/multi-server"
    mem_limit: 128m
    hostname: api
    environment:
      - REDIS_HOST=$REDIS_HOST
      - REDIS_PORT=$REDIS_PORT
      - PGUSER=$PGUSER
      - PGHOST=$PGHOST
      - PGDATABASE=$PGDATABASE
      - PGPASSWORD=$PGPASSWORD
      - PGPORT=$PGPORT
  worker:
    image: "a99nabilalqemam/multi-worker"
    mem_limit: 128m
    hostname: worker
    environment:
      - REDIS_HOST=$REDIS_HOST
      - REDIS_PORT=$REDIS_PORT
  nginx:
    image: "a99nabilalqemam/multi-nginx"
    mem_limit: 128m
    hostname: nginx
    ports:
      - "80:80"